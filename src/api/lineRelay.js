// Sends messages to LINE

module.exports = (translatedMessage) => {
    // TODO: Implement logic to send translated message to LINE
    console.log('Forwarding message to LINE:', translatedMessage);
};